#include "std.h"

class Solution {
public:
	int canCompleteCircuit(vector<int> &gas, vector<int> &cost) {
		priority_queue<int> que;
		int n = gas.size();
		for (int i = 0; i < n; i++)
		{

		}
	}
};
